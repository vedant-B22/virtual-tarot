services:
  - type: web
    name: live-tarot
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PASSWORD
        generateValue: true
      - key: VIDEO_PROVIDER
        value: webrtc_p2p
